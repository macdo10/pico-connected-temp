# pico-connected-temp
Pi Pico temperature probes updating web page
The pico queries a private php script that writes the sensor ID, temp and date/time to a MySQL database (easier than trying to get the pico to write directly to the database). The data is available on a different webpage. 

To do : write a script to update sensor names. 
